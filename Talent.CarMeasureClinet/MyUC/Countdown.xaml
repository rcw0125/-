<UserControl x:Class="Talent.CarMeasureClient.MyUC.Countdown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100"
             RenderTransformOrigin="0.5,0.5" Loaded="UserControl_Loaded">
    <UserControl.RenderTransform>
        <TransformGroup>
            <ScaleTransform  x:Name="scale" ></ScaleTransform>
        </TransformGroup>
    </UserControl.RenderTransform>
    <TextBlock x:Name="tb" Width="90" Height="90" Text="9" FontSize="90" TextAlignment="Center" FontWeight="Bold" FontFamily="FangSong" Foreground="Gold">
        <TextBlock.Effect>
            <DropShadowEffect Color="Orange"/>
        </TextBlock.Effect>
    </TextBlock>
</UserControl>
